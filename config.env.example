# =============================================================================
# Coherence Diagnostic Configuration
# =============================================================================
# Copy this file to config.env and edit the values.
# Environment variables take precedence over this file.
#
# For full documentation, see README.md
# =============================================================================


# =============================================================================
# Feature Toggle
# =============================================================================

# Enable gated access (email verification + admin panel)
# Set to 0 for open access (anyone can use the tool)
# Set to 1 for gated access (users must verify email, admin panel at /admin)
ENABLE_AUTH=0


# =============================================================================
# Required: OpenRouter API
# =============================================================================

# Your OpenRouter API key (required for Stage 3 diagnosis)
# Get one at: https://openrouter.ai/keys
OPENROUTER_API_KEY=


# =============================================================================
# Gated Access Configuration (only needed if ENABLE_AUTH=1)
# =============================================================================

# Secret key for signing session cookies (generate a random 64-char string)
# Example: openssl rand -hex 32
SESSION_SECRET=

# Password for accessing /admin panel
ADMIN_PASSWORD=

# Base URL for verification emails (your deployed URL)
BASE_URL=http://localhost:8000

# Maximum analyses per user account
MAX_ANALYSES_PER_USER=10

# Maximum new signups per day
MAX_NEW_USERS_PER_DAY=10


# =============================================================================
# SMTP Configuration (optional, only used if ENABLE_AUTH=1)
# =============================================================================
# If not configured, verification links are printed to console instead.
# Useful for local development.
#
# Works with any SMTP provider:
# - SendGrid: smtp.sendgrid.net, port 587, username "apikey", password is your API key
# - Mailgun: smtp.mailgun.org, port 587
# - AWS SES: email-smtp.{region}.amazonaws.com, port 587
# - Postmark: smtp.postmarkapp.com, port 587
# - Gmail: smtp.gmail.com, port 587 (requires app password)

# SMTP server hostname
SMTP_HOST=

# SMTP port (usually 587 for TLS, 465 for SSL, 25 for unencrypted)
SMTP_PORT=587

# SMTP username (often your email or "apikey" for services like SendGrid)
SMTP_USERNAME=

# SMTP password or API key
SMTP_PASSWORD=

# Sender email address
SMTP_FROM_EMAIL=noreply@yourdomain.com

# Sender display name
SMTP_FROM_NAME=Coherence Diagnostic
